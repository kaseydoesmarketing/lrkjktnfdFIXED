Upload Files & Environment Setup
bash# Upload these 3 files to your Replit:
# - api-routes.js
# - package.json  
# - server.js

# Your existing Stripe environment variables should already be set
# Only add these if missing:
Required Environment Variables (Add to Replit Secrets only if missing):
DATABASE_URL=your_postgresql_connection_string
GOOGLE_CLIENT_ID=your_existing_google_client_id  
GOOGLE_CLIENT_SECRET=your_existing_google_client_secret
OAUTH_REDIRECT_URI=https://your-replit-url.repl.co/api/auth/callback
NODE_ENV=production
PORT=3000

# Stripe variables (should already exist):
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_PUBLISHABLE_KEY=pk_test_...
# etc.
2. Replit Run Command
bashnpm install && npm start
3. CRITICAL: Test Your Existing OAuth First
Before doing anything else:

Start the server - Click Run in Replit
Test existing login immediately - Try your current Google login
If login breaks - Stop here and preserve your working OAuth code

4. The Real Problem We're Solving
Your frontend React components are making these API calls that don't exist yet:
javascript// From your dashboard code:
const { data: user } = useQuery({ queryKey: ['/api/auth/me'] });
const { data: stats } = useQuery({ queryKey: ['/api/dashboard/stats'] });  
const { data: tests } = useQuery({ queryKey: ['/api/tests'] });
const { data: recentVideos } = useQuery({ queryKey: ['/api/videos/recent'] });
The api-routes.js file creates these missing endpoints that your dashboard needs.
5. Step-by-Step Verification
✅ Step 1: Server Health Check
bash# Visit: https://your-replit-url.repl.co/api/health
# Should return: {"status":"healthy",...}
✅ Step 2: OAuth Preservation Test
bash# Test Google login exactly as before
# Dashboard should load without white screen
✅ Step 3: API Endpoints Now Work
bash# Check browser Network tab - these should return data:
/api/auth/me ✅ (instead of 404)
/api/dashboard/stats ✅ (instead of 404) 
/api/videos/recent ✅ (instead of 404)
/api/tests ✅ (instead of 404)
✅ Step 4: Stripe Still Working
bash# Your existing Stripe integration should be unchanged
# Payment flows should work exactly as before