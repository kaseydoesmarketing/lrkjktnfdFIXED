Fix the CreateTestModal to load real YouTube videos instead of demo data with a custom scrollbar.

Tasks:
1. In client/src/components/CreateTestModal.tsx:
   - Add useQuery import from @tanstack/react-query
   - Replace the hardcoded videos array with a useQuery hook that fetches from '/api/videos/channel'
   - Add search functionality with a search input
   - Make the video list scrollable with max-height of 400px and custom scrollbar
   - Add these classes to the scrollable div: "scrollbar-thin scrollbar-thumb-gray-300 scrollbar-track-gray-100"
   - Add loading and error states
   - Format view counts (1.2K, 1.5M) and dates properly

2. In server/routes.ts:
   - Add a new GET endpoint: /api/videos/channel
   - Use the YouTube API to fetch the user's uploaded videos
   - Include video statistics, thumbnails, and metadata
   - Return formatted JSON with id, title, thumbnail, viewCount, publishedAt, duration

3. In client/src/index.css or client/src/globals.css, add custom scrollbar styles:
   ```css
   /* Custom Scrollbar */
   .scrollbar-thin {
     scrollbar-width: thin;
   }
   
   .scrollbar-thumb-gray-300::-webkit-scrollbar-thumb {
     background-color: #d1d5db;
     border-radius: 4px;
   }
   
   .scrollbar-track-gray-100::-webkit-scrollbar-track {
     background-color: #f3f4f6;
     border-radius: 4px;
   }
   
   ::-webkit-scrollbar {
     width: 8px;
     height: 8px;
   }
   
   ::-webkit-scrollbar-thumb:hover {
     background-color: #9ca3af;
   }

Make sure:

Videos load when modal opens
Custom scrollbar appears on the video list (thin gray scrollbar)
Search filters videos in real-time
Clicking a video selects it and moves to the configure tab
Handle cases where user has no videos
Thumbnails use YouTube's CDN URLs



The modal should show real YouTube videos from the authenticated user's channel with a styled scrollbar, not demo data.

This now includes:
- The scrollbar CSS classes to add to the video list container
- The complete CSS code for the custom scrollbar
- Where to add the CSS (in the global styles file)
- The hover effect for better UX

The custom scrollbar will be thin, gray, and match the modal's clean aesthetic!